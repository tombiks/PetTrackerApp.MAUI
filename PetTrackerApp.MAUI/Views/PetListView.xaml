<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:lang="clr-namespace:PetTrackerApp.MAUI.Resources.Languages"
             x:Class="PetTrackerApp.MAUI.Views.PetListView"             
             Title="PetListView"
             NavigationPage.HasNavigationBar="False">

    <!-- Dev başlık -->
    <Grid x:Name="ExpandedHeader"
      HeightRequest="250"
      VerticalOptions="Start"
      RowDefinitions="Auto, *">

        <Grid.Shadow>
            <Shadow Brush="Black"
                Opacity="0.5"
                Offset="0,5"
                Radius="10" />
        </Grid.Shadow>

        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#667EEA" Offset="0.0" />
                <GradientStop Color="#764BA2" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Command="" button command buraya gelecek - ayarlar butonu -->
        <Button Grid.Row="0"             
            ImageSource="icon_option.png"
            BackgroundColor="#40FFFFFF"
            CornerRadius="22"
            WidthRequest="40"
            HeightRequest="40"
            HorizontalOptions="Start"
            VerticalOptions="Start"
            Margin="10,10,0,0"
            SemanticProperties.Description="Dil ve Uygulama Ayarları"
            SemanticProperties.Hint="Uygulama ayarlarını açar">
            <!--<Button.Behaviors>
                <toolkit:TouchBehavior
                Command="{Binding SelectPetCommand}"
                PressedOpacity="0.6"
                PressedScale="0.98" />
            </Button.Behaviors>-->            
        </Button>
        
        <!-- Uygulama logosu ve slogonu -->
        <VerticalStackLayout Grid.Row="1"
                         Spacing="0"
                         HorizontalOptions="Center"
                         VerticalOptions="Center"
                         Margin="0,-80,0,0">
            <Image Source="{StaticResource PetPlus_Logo}"
               Aspect="AspectFit"
               WidthRequest="200"
               HeightRequest="200"
               HorizontalOptions="Center"
               SemanticProperties.Description="Uygulamanın logosu" />

            <Image Source="{StaticResource PetPlus_Text}"
               Aspect="AspectFit"
               WidthRequest="200"
               HeightRequest="105"
               HorizontalOptions="Center"
               Margin="0,-80,0,0"
               SemanticProperties.Description="Uygulamanın text gorseli"/>

            <!-- Slogan //buraya slogan geliyor (her dilde yaziliyor mu daha test edilmedi) -->
            <Label Text="{x:Static lang:AppResources.App_Slogan}"
               FontSize="15"
               TextColor="White"
               HorizontalOptions="Center"
               HorizontalTextAlignment="Center"
               Margin="0,-20,0,0"
               SemanticProperties.Description="Uygulamanın slogani" />

        </VerticalStackLayout>
    </Grid>

    <!--<CollectionView.ItemTemplate>
        <DataTemplate x:DataType="models:Pet">

            <Border StrokeThickness="3"
                Stroke="#E8E8E8"
                BackgroundColor="White">
                --><!-- TouchBehavior dış Border için --><!--
                <Border.Behaviors>
                    <toolkit:TouchBehavior
                    BindingContext="{Binding Source={x:Reference Page}, Path=BindingContext}"
                    Command="{Binding GoToPetDetailCommand}"
                    CommandParameter="{Binding .}"
                    PressedOpacity="0.95"
                    PressedScale="0.995" />
                </Border.Behaviors>

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="24"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="Black" Opacity="0.06" Radius="8" Offset="0,2"/>
                </Border.Shadow>

                <Grid RowDefinitions="*,Auto" Padding="16">

                    <Grid Grid.Row="0" RowDefinitions="Auto,*">

                        --><!-- Edit butonu: kendi TouchBehavior'ı --><!--
                        <Border StrokeThickness="0"
                            BackgroundColor="#E3F2FD"
                            HorizontalOptions="End"
                            Padding="0"
                            Margin="0"
                            >
                            <Border.Behaviors>
                                <toolkit:TouchBehavior
                                BindingContext="{Binding Source={x:Reference Page}, Path=BindingContext}"
                                Command="{Binding SelectPetCommand}"
                                CommandParameter="{Binding .}"
                                PressedOpacity="0.85"
                                PressedScale="0.98" />
                            </Border.Behaviors>

                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10"/>
                            </Border.StrokeShape>

                            <Image Source="icon_edit.png"
                               WidthRequest="45"
                               HeightRequest="45"
                               VerticalOptions="Center"/>
                        </Border>

                        <Grid Grid.Row="1"
                          HorizontalOptions="Center"
                          VerticalOptions="Center"
                          Margin="0,8">

                            <Border WidthRequest="110"
                                HeightRequest="110"
                                StrokeThickness="4"
                                Stroke="#F0F0F0"
                                BackgroundColor="#F8F9FA">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="55"/>
                                </Border.StrokeShape>

                                <Grid>
                                    <Image Source="icon_default.png"
                                       Aspect="AspectFit"
                                       WidthRequest="110"
                                       HeightRequest="110"
                                       IsVisible="{Binding PicturePath, Converter={StaticResource IsNullOrEmptyConverter}}"/>

                                    <Image Source="{Binding PictureImageSource}"
                                       Aspect="AspectFill"
                                       IsVisible=""/>
                                </Grid>
                            </Border>

                            <Border WidthRequest="36"
                                HeightRequest="36"
                                BackgroundColor="White"
                                StrokeThickness="2"
                                Stroke="#F0F0F0"
                                HorizontalOptions="End"
                                VerticalOptions="End"
                                Margin="0,0,2,-5">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="18"/>
                                </Border.StrokeShape>
                                <Border.Shadow>
                                    <Shadow Brush="Black" Opacity="0.1" Radius="4" Offset="0,2"/>
                                </Border.Shadow>

                                <Image Source="{Binding IconImageSource}"
                                   Aspect="AspectFit"
                                   WidthRequest="32"
                                   HeightRequest="32"/>
                            </Border>
                        </Grid>
                    </Grid>

                    <VerticalStackLayout Grid.Row="1"
                                     Spacing="8"
                                     Margin="0,16,0,0">

                        <Label Text="{Binding Name}"
                           FontSize="17"
                           FontAttributes="Bold"
                           TextColor="#2D3436"
                           HorizontalTextAlignment="Center"
                           LineBreakMode="TailTruncation"
                           MaxLines="1"/>

                        <Border StrokeThickness="0"
                            HorizontalOptions="Center"
                            Padding="12,6"
                            BackgroundColor="{Binding PetGender, Converter={StaticResource GenderToColorConverter}}">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12"/>
                            </Border.StrokeShape>

                            <HorizontalStackLayout Spacing="8">
                                <Label Text="{Binding LocalizedPetType}"
                                   FontSize="11"
                                   FontAttributes="Bold"
                                   TextColor="White"
                                   LineBreakMode="TailTruncation"
                                   MaxLines="1"
                                   MaximumWidthRequest="90"
                                   VerticalOptions="Center"/>

                                <Label Text="{Binding PetGender, Converter={StaticResource GenderToSymbolConverter}}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="White"/>
                            </HorizontalStackLayout>
                        </Border>

                        <Border StrokeThickness="0"
                            HorizontalOptions="Center"
                            Padding="12,6"
                            BackgroundColor="#8FE37D">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12"/>
                            </Border.StrokeShape>
                            <HorizontalStackLayout HorizontalOptions="Center" Spacing="4">
                                <Label Text="🎂" FontSize="14"
                                   VerticalOptions="Center"/>
                                <Label Text="{Binding BirthDate, StringFormat='{0:dd/MM/yyyy}'}"
                                   TextColor="White"
                                   FontSize="13"
                                   VerticalOptions="Center"/>
                            </HorizontalStackLayout>
                        </Border>

                    </VerticalStackLayout>
                </Grid>
            </Border>
        </DataTemplate>
    </CollectionView.ItemTemplate>-->


</ContentPage>